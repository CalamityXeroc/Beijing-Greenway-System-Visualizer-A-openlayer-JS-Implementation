# 北京绿道系统移动端开发技术架构文档

## 📋 项目概述与目标

### 项目背景
北京绿道系统Web应用已开发完成，基于Vue 3 + OpenLayers + Mapbox技术栈。现需将其扩展为移动端应用，优化移动体验，并通过Capacitor打包为原生Android/iOS应用。

### 核心目标
1. **移动端体验优化**：重构Web端响应式设计，适配移动端触控交互
2. **Capacitor集成**：配置项目支持Android/iOS应用打包
3. **App新UI设计**：为移动端设计全新的界面和交互
4. **VSCode调试配置**：配置移动应用调试环境

### 操作规范
你需要使用我提供的skill来执行任务，你可以进入[text](skills)来自己选择合适的skill

## 🏗️ 技术架构总览

### 当前项目结构
详情见[text](README.md)

### 目标技术架构
```
greenway-vue/
├── capacitor.config.ts        # Capacitor配置文件
├── android/                   # Android原生项目（Capacitor生成）
├── ios/                      # iOS原生项目（Capacitor生成）
├── src/
│   ├── mobile/               # 移动端专用组件和逻辑
│   │   ├── components/       # 移动端组件
│   │   ├── hooks/           # 移动端Hooks
│   │   └── styles/          # 移动端样式
│   └── views/
│       ├── MobileMapView.vue # 移动端地图视图
│       └── ...              # 其他适配后的视图
├── public/
│   └── icons-splash/         # 应用图标和启动图
└── ...
```

## 🔧 详细技术需求

### 第一部分：移动端Web体验优化

#### 1.1 响应式设计重构
**任务**：优化现有Web应用在移动设备上的显示效果

**技术要点**：
- 使用CSS媒体查询适配不同屏幕尺寸
- 重构地图容器尺寸，确保在移动设备上全屏显示
- 调整UI元素（按钮、弹窗、菜单）的触摸目标大小（最小44×44px）
- 优化字体大小和行高，提高移动端可读性
- 利用[text](skills/vue-skills)和[text](skills/web-artifacts-builder)还有[text](skills/web-artifacts-builder)，[text](skills/frontend-design)来重构整个web系统

**预期改动文件**：
- `src/App.vue` - 添加移动端检测和适配逻辑
- `src/assets/main.css` - 添加移动端专用样式
- 所有视图组件 - 调整布局和样式

#### 1.2 触控交互优化
**任务**：优化地图在移动端的交互体验

**技术要点**：
- 调整OpenLayers交互参数，适应触摸操作
- 添加移动端手势支持（双指缩放、长按等）
- 优化弹窗显示，使用底部抽屉式设计
- 添加滑动返回等移动端常见交互


### 第二部分：Capacitor集成与配置

#### 2.1 Capacitor环境配置
**任务**：在现有Vue项目中集成Capacitor

**步骤**：
1. 安装Capacitor核心依赖
2. 初始化Capacitor配置
3. 添加Android和iOS平台支持
4. 配置应用图标和启动图

**详细操作**：
```bash
# 1. 安装Capacitor CLI和核心包
npm install @capacitor/core @capacitor/cli

# 2. 初始化Capacitor
npx cap init "北京绿道" "com.beijing.greenway" --web-dir=dist

# 3. 安装平台相关包
npm install @capacitor/android @capacitor/ios

# 4. 添加平台支持
npx cap add android
npx cap add ios

# 5. 安装常用插件
npm install @capacitor/app @capacitor/geolocation @capacitor/status-bar @capacitor/splash-screen
```

#### 2.2 Capacitor配置文件
**任务**：创建和配置Capacitor配置文件

#### 2.3 移动端原生功能集成
**任务**：添加移动端原生功能支持

**实现**：
```typescript
// src/utils/capacitor.ts
import { Capacitor } from '@capacitor/core';
import { Geolocation } from '@capacitor/geolocation';
import { App } from '@capacitor/app';
import { StatusBar, Style } from '@capacitor/status-bar';

export class MobileCapacitor {
  static isNativePlatform(): boolean {
    return Capacitor.isNativePlatform();
  }

  static async getCurrentLocation() {
    if (!this.isNativePlatform()) {
      return this.getBrowserLocation();
    }

    try {
      const coordinates = await Geolocation.getCurrentPosition({
        enableHighAccuracy: true,
        timeout: 10000
      });
      return {
        lng: coordinates.coords.longitude,
        lat: coordinates.coords.latitude,
        accuracy: coordinates.coords.accuracy
      };
    } catch (error) {
      console.error('获取位置失败:', error);
      return null;
    }
  }

  static async setupStatusBar() {
    if (this.isNativePlatform()) {
      await StatusBar.setStyle({ style: Style.Dark });
      await StatusBar.setBackgroundColor({ color: '#4CAF50' });
    }
  }

  private static getBrowserLocation(): Promise<{lng: number, lat: number}> {
    return new Promise((resolve, reject) => {
      if (!navigator.geolocation) {
        reject(new Error('浏览器不支持地理位置'));
      }

      navigator.geolocation.getCurrentPosition(
        (position) => {
          resolve({
            lng: position.coords.longitude,
            lat: position.coords.latitude
          });
        },
        (error) => {
          reject(error);
        }
      );
    });
  }
}
```

### 第三部分：VSCode调试环境配置

#### 3.1 VSCode调试配置
**任务**：配置VSCode支持移动端调试

#### 3.2 扩展推荐
**任务**：安装必要的VSCode扩展

**扩展列表**：
- Vue.volar (Vue语言支持)
- bradlc.vscode-tailwindcss (Tailwind CSS支持)
- capaj.vscode-exports-autocomplete (自动完成)
- christian-kohler.npm-intellisense (npm包智能提示)
- msjsdiag.debugger-for-chrome (Chrome调试)
- redhat.vscode-yaml (YAML支持)

### 第四部分：移动端新UI设计

#### 4.1 设计系统建立（主要）
**任务**：创建移动端专用设计系统

**任务**：一下是我对于app功能与样式的具体需求
1.样式与配色可按照web端的配色来做，以绿色基调为主，通过不同深浅的绿色和其他浅色进行设计，色调可参考[text](skills/vue-skills)，[text](skills/webapp-testing)和[text](skills/web-artifacts-builder)，[text](skills/frontend-design)等技能。
整体架构图：
┌─────────────────────────────────────────────────────┐
│                   移动端应用 (App)                   │
│   ┌─────────────┐ ┌─────────────┐ ┌─────────────┐  │
│   │   启动动画   │ │   主地图    │ │  绿道列表   │  │
│   └─────────────┘ └─────────────┘ └─────────────┘  │
│   ┌─────────────┐ ┌─────────────┐ ┌─────────────┐  │
│   │ 绿道详情页  │ │   登录/注册  │ │  个人中心   │  │
│   └─────────────┘ └─────────────┘ └─────────────┘  │
└─────────────────────────────────────────────────────┘
                            │
                    HTTP/WebSocket
                            │
┌─────────────────────────────────────────────────────┐
│                 Node.js 后端 API                     │
│  ┌──────────┐ ┌──────────┐ ┌──────────────┐        │
│  │ 用户认证  │ │ 绿道数据 │ │  地理位置    │        │
│  │  模块     │ │  模块    │ │   服务       │        │
│  └──────────┘ └──────────┘ └──────────────┘        │
└─────────────────────────────────────────────────────┘
                            │
                    SQL 查询/存储过程
                            │
┌─────────────────────────────────────────────────────┐
│              PostgreSQL + PostGIS 数据库             │
│  ┌──────────┐ ┌──────────┐ ┌──────────────┐        │
│  │  用户表   │ │ 绿道信息 │ │  用户收藏    │        │
│  │  (users) │ │ (trails) │ │  (favorites) │        │
│  └──────────┘ └──────────┘ └──────────────┘        │
└─────────────────────────────────────────────────────┘

2.我需要进入这个app后，有一个进入动画与"绿脉伴旅"文字与相关图画作为启动界面，进入界面后，可以直接看到地图与绿道的主体部分，导航栏在app的最下面，点击进入另一个界面后，可以看到所有绿道的列表，用户可以点击自己感兴趣的绿道来浏览具体的绿道界面，界面里有各自绿道的地图。
（1）启动动画界面
主页面结构：
┌─────────────────────────────────┐
│                                 │
│       [绿脉伴旅Logo动画]         │
│                                 │
│          "绿脉伴旅"             │
│          探索北京绿道之美        │
│                                 │
│        [加载进度条/动画]         │
│                                 │
└─────────────────────────────────┘

功能要点：
- 3-5秒的品牌展示动画
- 渐进式加载应用资源
- 检查用户登录状态
- 动画结束后自动跳转到主地图界面
(2)地图主界面
页面结构：
┌─────────────────────────────────┐
│ [返回] 北京绿道        [菜单]    │
├─────────────────────────────────┤
│                                 │
│          地图区域                │
│      （全屏展示，绿道高亮）       │
│                                 │
├─────────────────────────────────┤
│ [列表] [定位] [搜索] [收藏]      │
└─────────────────────────────────┘

功能要点：
- 全屏地图展示，绿道路线高亮显示
- 底部导航栏快速切换功能
- 定位按钮：快速定位到用户当前位置
- 搜索按钮：打开搜索界面
- 绿道点击：显示该绿道简要信息卡片
- 卡片点击：跳转到绿道详情页

（3）绿道列表界面
页面结构：
┌─────────────────────────────────┐
│ [返回] 绿道列表        [筛选]    │
├─────────────────────────────────┤
│ 推荐给您的绿道                  │
│ ┌─────────────┐ ┌─────────────┐ │
│ │  图片       │ │ 朝阳公园绿道 │ │
│ │             │ │  ★★★★☆     │ │
│ │             │ │  3.2km | 1.2km││
│ └─────────────┘ └─────────────┘ │
│                                 │
│ 所有绿道                        │
│ ┌─────────────────────────────┐ │
│ │ 温榆河绿道                  │ │
│ │ 全长12.5km  ★★★★☆         │ │
│ └─────────────────────────────┘ │
│ │ 奥林匹克森林公园绿道         │ │
│ │ 全长8.3km   ★★★★☆          │ │
│ └─────────────────────────────┘ │
│                                 │
└─────────────────────────────────┘

功能要点：
- 顶部显示用户当前位置
- "推荐给您的绿道"：基于位置智能推荐
- "所有绿道"：完整绿道列表
- 列表项显示：图片、名称、评分、距离、长度
- 点击列表项：进入绿道详情页
- 筛选功能：按距离、评分、难度等筛选

（4）绿道详情界面
页面结构：
┌─────────────────────────────────┐
│ [返回] 温榆河绿道      [收藏]    │
├─────────────────────────────────┤
│  ┌─────────────────────────┐    │
│  │     绿道专属地图         │    │
│  │    （显示完整路线）      │    │
│  └─────────────────────────┘    │
│                                 │
│ 基本信息                        │
│ · 长度：12.5公里                │
│ · 预计耗时：3-4小时            │
│ · 难度：中等                    │
│ · 起点：立水桥                  │
│ · 终点：沙河水库                │
│                                 │
│ 特色介绍                        │
│ 温榆河绿道是北京市重要的生态... │
│                                 │
│ 服务设施                        │
│ ✓ 卫生间  ✓ 停车场  ✓ 休息区   │
│                                 │
│ [导航至此] [分享]              │
└─────────────────────────────────┘

功能要点：
- 顶部返回按钮和收藏按钮
- 该绿道的专属地图展示
- 详细的绿道信息展示
- 服务设施图标化显示
- 行动按钮：导航、分享

（4）用户登录/注册界面

3.系统方面
在我提供的项目文档中，数据库架构部分已经明确设计了用户表（users表），而 API 接口​ 部分也预留了用户认证相关的端点。以下是基于您现有技术栈的具体实施方案：

（1）数据库层面：在 PostgreSQL 中创建用户表
需要在现有的 greenway_db数据库中，创建结构的表来安全地存储用户凭证：
是的，完全可以继续使用您现有的 PostgreSQL 数据库来存储用户的账号和密码。这是一个标准、安全且成熟的做法。
在您提供的项目文档中，数据库架构部分已经明确设计了用户表（users表），而 API 接口​ 部分也预留了用户认证相关的端点。以下是基于您现有技术栈的具体实施方案：
关键安全实践：
永远不要存储明文密码。password_hash字段用于存储使用 bcrypt​ 或 Argon2​ 等强哈希算法生成的加密字符串。
使用唯一约束（UNIQUE）防止用户名和邮箱重复。

（2）后端层面：在我的 Node.js 服务中实现认证 API
我的 greenway-backend需要新增处理用户注册、登录和鉴权的接口。
核心API端点示例：
POST /api/auth/register：接收用户名、邮箱、密码，进行验证后，将哈希后的密码存入 users表。
POST /api/auth/login：验证用户名/邮箱和密码。成功后，生成一个 JSON Web Token​ 返回给客户端。
GET /api/auth/profile（受保护）：客户端在请求头中携带 JWT，服务器验证令牌后返回对应用户的个人信息。

（3）前端/App层面：处理登录状态
用户成功登录后，将后端返回的 JWT​ 安全地存储起来（例如，使用 localStorage、sessionStorage或 Capacitor 的 Preferences插件）。
在后续需要认证的 API 请求中（如获取个人收藏、更新资料），在 HTTP 请求头（通常是 Authorization: Bearer <your-jwt-token>）中携带此令牌。
利用 Vue Router 的路由守卫功能，保护需要登录才能访问的页面（如个人中心、收藏页）。

4.技术栈
（1）前端技术栈
框架: Vue 3 + Composition API + TypeScript
状态管理: Pinia
路由: Vue Router 4
UI组件: 自定义移动端组件库 + Vant Mobile UI（可选）
地图: Mapbox GL JS / OpenLayers
HTTP客户端: Axios
跨平台: Capacitor 5.x
样式: CSS3 + Flexbox/Grid + CSS变量

（2）后端技术栈（现有）
运行时: Node.js 18+
框架: Express.js / Koa
数据库: PostgreSQL + PostGIS
认证: JWT + bcrypt
API文档: Swagger/OpenAPI 3.0

**文件**: `src/mobile/styles/design-system.css`


#### 4.2 移动端组件库
**任务**：创建移动端专用Vue组件

**组件清单**：
1. `MobileBottomNavigation` - 底部导航栏
2. `MobileSearchBar` - 搜索栏
3. `MobileDrawer` - 侧边抽屉
4. `MobileCard` - 卡片组件
5. `MobileFloatingButton` - 悬浮按钮
6. `MobileBottomSheet` - 底部动作面板
等等……

**示例组件**: `src/mobile/components/MobileBottomNavigation.vue`
```vue
<template>
  <nav class="mobile-bottom-nav" :class="{ 'with-safe-area': hasSafeArea }">
    <div class="nav-items">
      <button
        v-for="item in items"
        :key="item.id"
        class="nav-item"
        :class="{ 'active': activeItem === item.id }"
        @click="handleClick(item)"
      >
        <i :class="item.icon" class="nav-icon"></i>
        <span class="nav-label">{{ item.label }}</span>
      </button>
    </div>
  </nav>
</template>

<script setup>
import { ref, computed } from 'vue'

const props = defineProps({
  items: {
    type: Array,
    default: () => [
      { id: 'map', label: '地图', icon: 'fas fa-map' },
      { id: 'trails', label: '绿道', icon: 'fas fa-route' },
      { id: 'favorites', label: '收藏', icon: 'fas fa-heart' },
      { id: 'profile', label: '我的', icon: 'fas fa-user' }
    ]
  },
  initialActive: {
    type: String,
    default: 'map'
  }
})

const activeItem = ref(props.initialActive)
const hasSafeArea = computed(() => {
  return CSS.supports('padding-bottom: env(safe-area-inset-bottom)')
})

const handleClick = (item) => {
  activeItem.value = item.id
  emit('change', item)
}

const emit = defineEmits(['change'])
</script>

<style scoped>
.mobile-bottom-nav {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  background: var(--color-surface);
  border-top: 1px solid rgba(0, 0, 0, 0.1);
  padding: var(--space-sm) var(--space-md);
  box-shadow: var(--shadow-large);
  z-index: 1000;
}

.mobile-bottom-nav.with-safe-area {
  padding-bottom: calc(var(--space-sm) + env(safe-area-inset-bottom));
}

.nav-items {
  display: flex;
  justify-content: space-around;
  align-items: center;
}

.nav-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: var(--space-xs) var(--space-sm);
  background: transparent;
  border: none;
  color: var(--color-text-secondary);
  transition: all 0.2s ease;
  min-height: 60px;
  min-width: 60px;
}

.nav-item.active {
  color: var(--color-primary);
}

.nav-item:active {
  transform: scale(0.95);
}

.nav-icon {
  font-size: var(--text-lg);
  margin-bottom: var(--space-xs);
}

.nav-label {
  font-size: var(--text-xs);
  font-weight: 500;
}
</style>
```

### 第五部分：构建和部署脚本

#### 5.1 构建脚本
**任务**：创建自动化构建脚本

**文件**: `scripts/build-mobile.js`
```javascript
#!/usr/bin/env node

const { execSync } = require('child_process')
const fs = require('fs')
const path = require('path')

console.log('🚀 开始构建移动端应用...')

// 步骤1: 构建Vue应用
console.log('📦 构建Vue应用...')
execSync('npm run build', { stdio: 'inherit' })

// 步骤2: 同步到Capacitor
console.log('🔄 同步到Capacitor...')
execSync('npx cap sync', { stdio: 'inherit' })

// 步骤3: 复制资源文件
console.log('📁 复制资源文件...')
const resourcesDir = path.join(__dirname, '../resources')
if (fs.existsSync(resourcesDir)) {
  execSync(`npx cap copy android`, { stdio: 'inherit' })
  execSync(`npx cap copy ios`, { stdio: 'inherit' })
}

// 步骤4: 打开IDE（可选）
console.log('🛠️  构建完成！')
console.log('📱 下一步操作：')
console.log('1. 打开Android Studio: npx cap open android')
console.log('2. 打开Xcode: npx cap open ios')
console.log('3. 构建APK: cd android && ./gradlew assembleDebug')
```

#### 5.2 包管理配置
**任务**：更新package.json支持移动端构建

**文件**: `package.json`更新部分
```json
{
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc && vite build",
    "preview": "vite preview",
    "mobile:init": "npm install @capacitor/core @capacitor/cli && npx cap init",
    "mobile:add-android": "npm install @capacitor/android && npx cap add android",
    "mobile:add-ios": "npm install @capacitor/ios && npx cap add ios",
    "mobile:sync": "npm run build && npx cap sync",
    "mobile:open-android": "npx cap open android",
    "mobile:open-ios": "npx cap open ios",
    "mobile:build-android": "npm run build && npx cap sync android && cd android && ./gradlew assembleDebug",
    "mobile:run-android": "npx cap run android",
    "mobile:run-ios": "npx cap run ios"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0",
    "@capacitor/android": "^5.0.0",
    "@capacitor/ios": "^5.0.0"
  },
  "dependencies": {
    "@capacitor/core": "^5.0.0",
    "@capacitor/app": "^5.0.0",
    "@capacitor/geolocation": "^5.0.0",
    "@capacitor/status-bar": "^5.0.0",
    "@capacitor/splash-screen": "^5.0.0"
  }
}
```

## 🚀 实施步骤总结

### 第1阶段：环境准备和基础配置
1. 安装Capacitor和相关插件
2. 初始化Capacitor配置
3. 添加Android和iOS平台支持

### 第2阶段：移动端Web优化
1. 重构响应式设计
2. 优化触控交互
3. 测试移动端浏览器体验

### 第3阶段：原生功能集成
1. 集成GPS定位功能
2. 配置状态栏和启动屏
3. 添加移动端原生插件

### 第4阶段：新UI实现
1. 创建设计系统
2. 开发移动端专用组件
3. 应用新UI到所有页面

### 第5阶段：调试和构建
1. 配置VSCode调试环境
2. 创建构建脚本
3. 测试APK构建和安装

## ⚠️ 注意事项和依赖项

### 环境要求
- Node.js 16+
- Java JDK 11+ (Android)
- Xcode 13+ (iOS)
- Android Studio (Android开发)

### 项目依赖
确保以下文件存在：
Q: 我的public/index.html包含Mapbox和FontAwesome CDN链接
Q: 我的vite.config.js已正确配置Vue 3项目
Q: 我的package.json包含Vue 3和TypeScript依赖

### 权限配置
**Android**: `android/app/src/main/AndroidManifest.xml`
```xml
<uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
<uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
```

**iOS**: `ios/App/App/Info.plist`
```xml
<key>NSLocationWhenInUseUsageDescription</key>
<string>需要您的位置来展示附近的绿道</string>
<key>NSLocationAlwaysUsageDescription</key>
<string>需要您的位置来提供绿道导航服务</string>
```

## 🔍 测试验证清单

完成以上步骤后，请验证：

### Web端验证
- [ ] 在手机浏览器上访问，页面布局正常
- [ ] 地图触摸交互流畅
- [ ] 移动端组件显示正确

### Android验证
- [ ] APK可以正常打包
- [ ] 应用可以安装到Android设备
- [ ] GPS定位功能正常
- [ ] 启动屏和状态栏正常显示

### iOS验证
- [ ] 可以在Xcode中打开项目
- [ ] 可以在模拟器中运行
- [ ] 触控交互正常

### 调试验证
- [ ] VSCode可以连接到移动设备调试
- [ ] 可以设置断点和查看变量
- [ ] 控制台输出正常

### 文档说明
只需生成一个最终文档用来汇报所有的信息，不需要生成额外的任何文档，有什么事情可以直接告诉我。